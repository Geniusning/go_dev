package main

func modify(a []int) {
	a[0] = 1000
	a[1] = 999
}
func main() {
	// var slice []int
	// var arr [5]int = [...]int{3, 4, 5, 6, 3}
	// slice = arr[0:1]
	// fmt.Println(slice)
	// fmt.Println(len(slice))
	// fmt.Println(cap(slice))
	// slice = slice[0:1]
	// fmt.Println(slice)
	// fmt.Println(len(slice))
	// fmt.Println(cap(slice))
}
